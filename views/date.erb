<!DOCTYPE html>
<html>
    <% date = DateTime.new %>
    <body>
        <h1>The date is <%=date.strftime("%A") date.strftime("%m")%>.</h1>
    </body>
</html>

 date.strftime("%j") date.strftime("%Y")